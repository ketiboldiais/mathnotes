<Head>
	<title>Reference</title>
	<meta name={`description`} content={`Cheatsheet of common math propositions.`}/>
</Head>

# Reference
_This is a reference of common mathematical propositions._

## Exponents
### Power of Zero
- Iff ${n \gt 0, }$ then ${0^n = 0.}$
- Iff ${n \lt 0,}$ then ${0^n = \text{undefined}.}$
	- Reason: If ${n \lt 0}$ and ${0^{n} = 0,}$ then ${0^{n} = 1/0.}$
- If ${n = 0,}$ then ${0^0}$ is either ${1}$ or undefined.


### Power of One
- ${1^n = 1.}$
- If ${n}$ is even, then ${(-1)^n = 1.}$
- If ${n}$ is odd, then ${(-1)^n = -1.}$


### Recursive Definition of Exponents
$$
b^n = 
\begin{cases}
	1 \space &\text{if} \space n = 0 \\
	b \space &\text{if} \space n = 1 \\
	b \times b^{n-1} \space &\text{else}
\end{cases}
$$

### Addition of Exponents
- ${b^{m+n} = b^m \times b^n.}$

### Negative Exponents
- ${b^{-n} = \dfrac{1}{b^n}.}$
- ${0^{-n} = \text{undefined}.}$

### Associativity of Exponents
- ${(b^m)^n = b^m \times b^n.}$

### Distributivity of Exponents
- ${(ab)^n = a^n \times b^n.}$

### Powers of Two
<table>
	<tbody>
		<tr>
			<td>${2^0}$</td>
			<td>${1}$</td>
			<td>${2^1}$</td>
			<td>${2}$</td>
		</tr>
		<tr>
			<td>${2^2}$</td>
			<td>${4}$</td>
			<td>${2^3}$</td>
			<td>${8}$</td>
		</tr>
		<tr>
			<td>${2^4}$</td>
			<td>${16}$</td>
			<td>${2^5}$</td>
			<td>${32}$</td>
		</tr>
		<tr>
			<td>${2^6}$</td>
			<td>${64}$</td>
			<td>${2^7}$</td>
			<td>${128}$</td>
		</tr>
		<tr>
			<td>${2^8}$</td>
			<td>${512}$</td>
			<td>${2^9}$</td>
			<td>${512}$</td>
		</tr>
		<tr>
			<td>${2^{10}}$</td>
			<td>${1024}$</td>
			<td>${2^{11}}$</td>
			<td>${2048}$</td>
		</tr>
		<tr>
			<td>${2^{12}}$</td>
			<td>${4096}$</td>
			<td>${2^{13}}$</td>
			<td>${8192}$</td>
		</tr>
		<tr>
			<td>${2^{14}}$</td>
			<td>${16~384}$</td>
			<td>${2^{15}}$</td>
			<td>${32~768}$</td>
		</tr>
		<tr>
			<td>${2^{16}}$</td>
			<td>${65~536}$</td>
			<td>${2^{17}}$</td>
			<td>${131~072}$</td>
		</tr>
		<tr>
			<td>${2^{18}}$</td>
			<td>${262~144}$</td>
			<td>${2^{19}}$</td>
			<td>${524~288}$</td>
		</tr>
		<tr>
			<td>${2^{20}}$</td>
			<td>${1~048~576}$</td>
			<td>${2^{21}}$</td>
			<td>${2~097~152}$</td>
		</tr>
		<tr>
			<td>${2^{32}}$</td>
			<td>${4~294~967~296}$</td>
			<td>${2^{64}}$</td>
			<td>${18~446~744~073~709~551~616}$ (over 18 quintillion)</td>
		</tr>
	</tbody>
</table>

### Rational Exponents
- Iff ${n \ge 0,}$ then ${x^{1/n} = \sqrt[n]{x}}$
- ${\sqrt[n]{x} \iff (y^n = x \land y \in \reals^+).}$
- If ${x \in \reals^+}$ and ${\frac{p}{q} \in \mathbb{Q},}$ then
$$
	x^{p/q} = (x^p)^{\frac{1}{q}} = \left(x^{\frac{1}{q}}\right)^p
$$

## Logarithms
### Definition
- ${\log_b(y) = x \iff b^x = y}$ where ${b \neq 0.}$
### Log of One
- Iff ${b \neq 0,}$ then ${\log_b(1) = 0}$ because ${b^0 = 1.}$
- ${\log_b(b) = 1,}$ because ${b^1 = b.}$
### Log of Products
- ${\log_b(xy) = \log_b(x) + \log_b(y).}$
### Log of Quotients
- ${\log_b\left( \frac{x}{y} \right) = \log_b(x) + \log_b(y)}$
	- Reason: ${b^c b^d = b^{c+d}.}$
- ${\log_b(x^d) = d \log_b(x)}$
	- Reason: ${(b^c)^d = b^{cd}.}$
- ${\log_b(\sqrt[n]{x}) = \dfrac{\log_b(x)}{y}}$
	- Reason: ${\sqrt[y]x = x^{1/y}.}$
- ${x^{\log_b(y)}=y^{\log_b(x)}.}$
	- Reason:
		$$
		\begin{aligned}
			x^{\log_b(y)} &= b^{\log_b(x)\log_b(y)} \\
			&= \left(b^{\log_b(y)}\right)^{\log_b(x)} \\
			&= y^{\log_b(x)}
		\end{aligned}
		$$
		

	
## Useful Scripts
### Empty Trash
~~~
osascript -e 'tell app "Finder" to empty'
~~~
### Lower Process Schedule Priority
Where ${A}$ is an application's name:
$$
~~~
for f in $(pgrep '𝐴'); do renice +20 -p $f; done
~~~
