import { Plot } from "../../../components/hago";

# Periodic Functions

_This chapter covers notes on periodic functions. The notes assume assume the reader is familiar with right-angle trigonometry. Notes on this topic may be found [here](../geometry/trigonometry)._

We know that any point ${(x,y) \in \reals^2}$ can be expressed as:

$$
(x=\cos \theta, y=\sin \theta)
$$

where ${\theta}$ is some fraction of ${2 \pi.}$ Substituting a value for
${\theta}$ in ${\cos \theta}$ gives us the number of steps we take along the
${x}$-axis, and substituting a value for ${\theta}$ in ${\sin \theta}$ gives us
the number of steps along the ${y}$-axis.

<Fig
link={"https://res.cloudinary.com/sublimis/image/upload/v1663116496/math/Unit_circle_angles_k0h2px.svg"}
imwidth={"750"} imheight={"732"} caption={"unit circle"} width={30} />

Using the ratio identities, we get the following relations.

<Grid cols={2}>

$$
	\begin{array}{:c:c:}
	\cos(t) = x & \sin(t) = y \\[2em]
	\tan(t) = \dfrac{y}{x},~x \neq 0 & \quad \csc(t) = \dfrac{1}{y},~y \neq 0 \\[2em]
	\sec(t) = \dfrac{1}{x},~x \neq 0 & \cot(t) = \dfrac{x}{y},~y \neq 0
	\end{array}
$$

<Fig
	link={"https://res.cloudinary.com/sublimis/image/upload/v1663118298/math/trig_functions_bpmj1o.svg"}
	imwidth={"176"}
	imheight={"203"}
	caption={"Trig functions"}
	width={40}
/>

</Grid>

Because the trigonometric ratios map real numbers (${\theta}$) to real
numbers, they are, in fact, functions. Collectively, they form a family of
functions called the _periodic functions_.

| ~function~     | ~domain~                                                   | ~common restriction~                          | ~codomain~                  |
| -------------- | ---------------------------------------------------------- | --------------------------------------------- | --------------------------- |
| ${\cos\px{x}}$ | ${\reals}$                                                 | ${\mi \frac{\pi}{2} \le x \le \frac{\pi}{2}}$ | ${-1 \le \sin\px{x} \le 1}$ |
| ${\sin\px{x}}$ | ${\reals}$                                                 | ${0 \le x \le \pi}$                           | ${-1 \le \sin\px{x} \le 1}$ |
| ${\tan\px{x}}$ | ${\reals \rid \frac{\pi}{2} + n\pi}$ where ${n \in \uint}$ | ${\mi\frac{\pi}{2} \lt x \lt \frac{\pi}{2}}$  | ${\reals}$                  |
| ${\sec\px{x}}$ | ${\reals \rid \frac{\pi}{2} + n\pi}$ where ${n \in \uint}$ | ${\frac{\pi}{2} \lt x \le \pi}$               | ${\reals \rid \oio{-1}{1}}$ |
| ${\csc\px{x}}$ | ${\reals \rid 0}$                                          | ${0 \lt x \lt \frac{\pi}{2}}$                 | ${\reals \rid \oio{-1}{1}}$ |
| ${\cot\px{x}}$ | ${\reals \rid n\pi,}$ where ${n \in \uint}$                | ${0 \lt x \lt \pi}$                           | ${\reals}$                  |







