# Docker

## Containers
> __~container~.__ A _container_ is pair ${(P,D),}$ where ${P}$ is a set of code and ${D}$ is a set of _dependencies_ needed to execute ${P.}$

~example~. NodeJS code and the NodeJS runtime.

~example~. Kotlin code and JVM.

~example~. QT code and a C${\mo{++}}$ compiler.

