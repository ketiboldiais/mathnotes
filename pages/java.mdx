
## Entry Point 
Java programs are, in short, classes. Single line comments begin with two forward-slashes `//` or delimited by `/* */`.

<Algo>

1. __public class__ program ${\lbrace}$ _// beginning of class definition_
	1. __public static void__ __main__ (__String__${\ix{~}}$ __args__) ${\lbrace}$ _// main method (entry point)_
		1. ${\textbf{system.out.println}(\string{hello, world!})}$; _// method instruction_
	2. ${\rbrace}$
2. ${\rbrace}$

</Algo>

